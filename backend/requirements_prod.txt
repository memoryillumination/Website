# Production host running app.py with APP_ENV=production. GPU inference is
# offloaded to the Modal-hosted worker (backend/flux_1_kontext_modal.py), so
# this host never loads torch/diffusers/etc. locally — Modal builds its own
# container image for that from the pip_install() list inside that file.

Flask>=3.0.0
Flask-CORS>=4.0.0
resend>=2.36.0
uWSGI>=2.0.24
argon2-cffi>=23.1.0
itsdangerous>=2.2.0
python-dotenv>=1.0.1
requests>=2.31.0
numpy>=1.26.0
# Image parsers are the largest untrusted-input surface in this app: they
# decode attacker-supplied bytes in native code. Pinned to a known-good
# floor and a tested ceiling — bump deliberately, and check release notes
# for security fixes rather than letting them drift.
opencv-python-headless>=4.13.0.92,<5.0.0  # Headless avoids unnecessary X11/GUI dependencies in a server VM
Pillow>=12.2.0,<13.0.0
pillow-heif>=1.5.0,<2.0.0        # HEIC/HEIF decoding (iPhone default photo format)
pillow-avif-plugin>=1.6.0,<2.0.0  # AVIF decoding (Android/Chrome default in some flows)
modal>=1.2.3                # Client library to dispatch featureB to the Modal GPU worker
