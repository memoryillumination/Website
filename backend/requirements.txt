# --- Intel Hardware Acceleration Index ---
--extra-index-url https://download.pytorch.org/whl/xpu

# --- Core Web Server & API (app.py) ---
Flask>=3.0.0
Flask-CORS>=4.0.0
Flask-Mail>=0.9.1
argon2-cffi>=23.1.0
python-dotenv>=1.0.1
numpy>=1.26.0
opencv-python-headless>=4.9.0.80  # Headless avoids unnecessary X11/GUI dependencies in a server VM
pillow-heif>=0.18.0        # HEIC/HEIF decoding (iPhone default photo format)
pillow-avif-plugin>=1.4.0  # AVIF decoding (Android/Chrome default in some flows)
modal>=1.2.3                # Only required when APP_ENV=production (routes featureB to the Modal GPU worker)

# --- AI Inference Stack (local_worker.py) ---
torch
torchvision
torchaudio
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
sentencepiece>=0.2.0
protobuf>=5.27.0
huggingface_hub>=0.24.0
Pillow>=10.3.0
